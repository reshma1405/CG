spring:
  application:
    name: Airport
  datasource:
    url: jdbc:mysql://localhost:3306/airport_db
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
server:
  port: 8080

